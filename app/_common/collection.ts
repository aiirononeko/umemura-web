export interface Customer {
  name: string;
  phoneNumber: string;
  email: string;
  password: string;
};

export interface Course {
  title: string;
  time: string;
  description: string;
  amount: number;
}

export interface Stuff {
  firstName: string,
  lastName: string,
  gender: string,
  profile: string,
  email: string,
  password: string,
};
